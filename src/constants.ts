import { Direction } from './interfaces';

export const START_VALUE = '@';
export const END_VALUE = 'x';
export const VERTICAL_VALUE = '|';
export const HORIZONTAL_VALUE = '-';
export const TURN_VALUE = '+';

export const ALL_DIRECTIONS: Direction[] = [Direction.Up, Direction.Right, Direction.Down, Direction.Left];
